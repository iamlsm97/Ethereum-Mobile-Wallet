import * as authActions from './authActions';
import * as ethActions from './ethActions';
import * as txActions from './txActions';

const Actions = {
  auth: authActions,
  eth: ethActions,
  tx: txActions,
};

export default Actions;
